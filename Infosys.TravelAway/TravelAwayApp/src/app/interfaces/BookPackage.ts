export interface IBookPackage {
  bookingId:number,
  contactNumber: string,
  emailId: string,
  address: string,
  dateOfTravel: Date,
  numberOfAdults: number,
  numberOfChildren: number,
  status: string,
  packageId:number
}
